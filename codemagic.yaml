workflows:
  android-release:
    name: Gauging App Build
    environment:
      vars:
        JAVA_VERSION: 17
    scripts:
      - name: Grant gradlew permission
        script: chmod +x ./gradlew
      - name: Build release APK
        script: ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/gauging/gauging.apk
